export type CalendarEvent = {
  cityName: string;
  date: Date;
  lat: number;
  lng: number;
  temperatureMax: number;
  temperatureMin: number;
  title: string;
  weatherDescr: string;
  weatherIcon: string;
  weatherId: number;
  weatherMain: string;
};
